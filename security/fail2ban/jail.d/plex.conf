[plex-media-server]
enabled  = true
filter   = plexms
port     = 32400
# Plex Docker stores logs under the bind mount shown below.
logpath  = /var/log/plexms/plexms.log

# Tuning
maxretry = 6
findtime = 10m
bantime  = 1h

# Leave allowlists to jail.d/plex.local overrides so site-specific networks stay out of Git

# Use iptables directly from a container (compatible with host or containerized deployment)
banaction = iptables-multiport
